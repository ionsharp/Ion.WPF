<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i="clr-namespace:Ion.Controls"
    xmlns:Data="clr-namespace:Ion.Data">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="{i:Style Key=Constants}"/>
        <ResourceDictionary Source="{i:Style Key=FrameworkElement}"/>
    </ResourceDictionary.MergedDictionaries>
    <Style x:Key="{x:Type Label}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Type FrameworkElement}}">
        <Setter Property="FontFamily" Value="{i:Bind FontFamily, AppSource=Data}"/>
        <Setter Property="Foreground" Value="{i:Theme Text}"/>
        <Setter Property="i:XControl.FontScale" Value="{i:Bind FontScale, AppSource=Data}"/>
        <Setter Property="i:XControl.FontScaleOrigin" Value="{i:Bind FontSize, AppSource=Data}"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding Border.CornerRadius}"
                        Padding="{TemplateBinding Padding}">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            RecognizesAccessKey="True"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Static i:XLabel.Accent}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Type Label}}">
        <Setter Property="Background" Value="{i:Theme Key=AccentMedium}"/>
        <Setter Property="Foreground" Value="{i:Theme AccentForeground}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Padding" Value="6,3"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.AccentLight}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Accent}}">
        <Setter Property="Background" Value="{i:Theme Key=AccentLight}"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.AccentLightMedium}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Accent}}">
        <Setter Property="Background" Value="{i:Theme Key=AccentLightMedium}"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.AccentMediumLight}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Accent}}">
        <Setter Property="Background" Value="{i:Theme Key=AccentMediumLight}"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.AccentMedium}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Accent}}"/>

    <Style x:Key="{x:Static i:XLabel.AccentMediumDark}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Accent}}">
        <Setter Property="Background" Value="{i:Theme Key=AccentMediumDark}"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.AccentDarkMedium}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Accent}}">
        <Setter Property="Background" Value="{i:Theme Key=AccentDarkMedium}"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.AccentDark}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Accent}}">
        <Setter Property="Background" Value="{i:Theme Key=AccentDark}"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.Header}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Type Label}}">
        <Setter Property="BorderBrush" Value="#11000000"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{i:Theme Accent}"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.Header1}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Header}}">
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="FontSize" Value="32"/>
        <Setter Property="Padding" Value="0,0,0,5"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.Header2}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Header}}">
        <Setter Property="FontSize" Value="28"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.Header3}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Header}}">
        <Setter Property="FontSize" Value="26"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.Header4}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Header}}">
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.Header5}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Header}}">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <Style x:Key="{x:Static i:XLabel.Header6}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Header}}">
        <Setter Property="Foreground" Value="DodgerBlue"/>
        <!--<Setter Property="Foreground" Value="LightSkyBlue"/>-->
        <!--<Setter Property="Foreground" Value="PowderBlue"/>-->
        <!--<Setter Property="Foreground" Value="SkyBlue"/>-->
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding Border.CornerRadius}"
                        Padding="{TemplateBinding Padding}">
                        <Grid i:XGrid.AutoColumns="True" i:XGrid.Columns="Auto,*">
                            <ContentPresenter Content="{i:Bind Content, From=TemplatedParent}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            <i:LineElement Margin="10,0,0,0" Orientation="Horizontal" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Static i:XLabel.Header7}" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Static i:XLabel.Header6}}">
        <Setter Property="Foreground" Value="#88000000"/>
        <Setter Property="FontSize" Value="11"/>
    </Style>
</ResourceDictionary>