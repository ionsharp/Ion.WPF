<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:l="http://wpflocalizeextension.codeplex.com"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:i="clr-namespace:Ion.Controls"
    xmlns:Data="clr-namespace:Ion.Data">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="{i:Style Key=ContentControl}"/>
        <ResourceDictionary Source="{i:Style Key=ItemsControl}"/>
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="{x:Type i:BarGraphItem}" TargetType="{x:Type i:BarGraphItem}" BasedOn="{StaticResource {x:Type ContentControl}}"/>

    <Style x:Key="{x:Type i:BarGraph}" TargetType="{x:Type i:BarGraph}" BasedOn="{StaticResource {x:Type ItemsControl}}">
        <Setter Property="Background" Value="#11000000"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Border.CornerRadius" Value="0"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{i:Theme Text}"/>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="i:BarGraphItem" BasedOn="{StaticResource {x:Type i:BarGraphItem}}">
                    <Setter Property="Width">
                        <Setter.Value>
                            <Data:MultiBind ConverterType="{x:Type i:BarGraphConverter}">
                                <i:Bind From="Self" Path="Value"/>
                                <Data:Ancestor FromType="{x:Type i:BarGraph}" Path="Maximum"/>
                                <Data:Ancestor FromType="{x:Type i:BarGraph}" Path="ActualWidth"/>
                            </Data:MultiBind>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>