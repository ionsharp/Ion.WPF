<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:l="http://wpflocalizeextension.codeplex.com"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i="clr-namespace:Ion.Controls"
    xmlns:Data="clr-namespace:Ion.Data"
    xmlns:Numbers="clr-namespace:Ion.Numeral;assembly=Ion"
    xmlns:System="clr-namespace:System;assembly=mscorlib">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="{i:Style Key=Control}"/>
    </ResourceDictionary.MergedDictionaries>
    <Style x:Key="{x:Type i:PointControl}" TargetType="{x:Type i:PointControl}" BasedOn="{StaticResource {x:Type Control}}">
        <Setter Property="Background" Value="#11000000"/>
        <Setter Property="BorderBrush" Value="#33000000"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Height" Value="{i:Bind From=Self, Path=Zoom}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type i:PointControl}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        ClipToBounds="False">
                        <Border.Resources>
                            <Data:Reference x:Key="PointControl" Data="{i:Bind From=TemplatedParent}"/>
                        </Border.Resources>
                        <Border.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="{i:Text Add}"
                                    Command="{i:Bind Data.AddCommand, Source={StaticResource PointControl}}"
                                    Icon="{i:Image Key=Plus}"/>
                            </ContextMenu>
                        </Border.ContextMenu>
                        <Grid ClipToBounds="False">
                            <Viewbox>
                                <Polygon Fill="#11000000"
                                    Points="{TemplateBinding Points}"
                                    Stroke="#22000000">
                                    <Polygon.StrokeThickness>
                                        <Data:MultiBind ConverterName="Zoom">
                                            <Binding>
                                                <Binding.Source>
                                                    <System:Double>2</System:Double>
                                                </Binding.Source>
                                            </Binding>
                                            <i:Bind From="TemplatedParent" Path="Zoom"/>
                                        </Data:MultiBind>
                                    </Polygon.StrokeThickness>
                                </Polygon>
                            </Viewbox>
                            <ItemsControl ItemsSource="{TemplateBinding MovablePoints}">
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="FrameworkElement">
                                        <Setter Property="Canvas.Left">
                                            <Setter.Value>
                                                <Data:MultiBind ConverterName="Math">
                                                    <Binding Path="X"/>
                                                    <Binding Source="{x:Static Numbers:Operator.Multiply}"/>
                                                    <Binding Path="Data.Zoom" Source="{StaticResource PointControl}"/>
                                                    <Binding Source="{x:Static Numbers:Operator.Subtract}"/>
                                                    <Binding>
                                                        <Binding.Source>
                                                            <System:Double>8</System:Double>
                                                        </Binding.Source>
                                                    </Binding>
                                                </Data:MultiBind>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Canvas.Top">
                                            <Setter.Value>
                                                <Data:MultiBind ConverterName="Math">
                                                    <Binding Path="Y"/>
                                                    <Binding Source="{x:Static Numbers:Operator.Multiply}"/>
                                                    <Binding Path="Data.Zoom" Source="{StaticResource PointControl}"/>
                                                    <Binding Source="{x:Static Numbers:Operator.Subtract}"/>
                                                    <Binding>
                                                        <Binding.Source>
                                                            <System:Double>8</System:Double>
                                                        </Binding.Source>
                                                    </Binding>
                                                </Data:MultiBind>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Ellipse
                                            Cursor="Hand"
                                            Fill="#00bb24"
                                            Height="16" Width="16"
                                            SnapsToDevicePixels="False"
                                            Stroke="#44000000"
                                            StrokeThickness="6"
                                            ToolTip="{i:Bind}">
                                            <Ellipse.ContextMenu>
                                                <ContextMenu DataContext="{i:Bind From=Self, Path=PlacementTarget.DataContext}">
                                                    <MenuItem Header="Insert after"
                                                        Command="{i:Bind Data.InsertAfterCommand, Source={StaticResource PointControl}}"
                                                        CommandParameter="{i:Bind}"/>
                                                    <MenuItem Header="Insert before"
                                                        Command="{i:Bind Data.InsertBeforeCommand, Source={StaticResource PointControl}}"
                                                        CommandParameter="{i:Bind}"/>
                                                    <Separator/>
                                                    <MenuItem Header="Remove"
                                                        Command="{i:Bind Data.RemoveCommand, Source={StaticResource PointControl}}"
                                                        CommandParameter="{i:Bind}"/>
                                                </ContextMenu>
                                            </Ellipse.ContextMenu>
                                        </Ellipse>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Width" Value="{i:Bind From=Self, Path=Zoom}"/>
    </Style>
</ResourceDictionary>