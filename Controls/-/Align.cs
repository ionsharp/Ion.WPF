namespace Ion;

/// <summary>Indicates where an element should be displayed on the horizontal axis relative to parent element.</summary>
public enum AlignX
{
    Left, Center, Right, Stretch
}

/// <summary>Indicates where an element should be displayed on the vertical axis relative to parent element.</summary>
public enum AlignY
{
    Top, Center, Bottom, Stretch
}