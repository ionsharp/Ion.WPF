namespace Ion.Data;

public enum GroupDefault
{
    Add, Command, Control, Copy, Data, Debug, Edit, Export, Format, General, Group, Import, Item, New, Option, Preset, Remove, Save, Search, Show, Sort, Type, View, Warn
}